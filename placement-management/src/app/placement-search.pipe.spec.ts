import { PlacementSearchPipe } from './placement-search.pipe';

describe('PlacementSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PlacementSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
